export default function Engine({ t }) {
  return (
    <section id="engine">
      <h2 className="text-4xl md:text-5xl font-bold mb-12 mt-4">
        {t("engine.title")}
      </h2>
    </section>
  );
}
